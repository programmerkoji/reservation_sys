# 予約管理システム（アプリ本体）

このディレクトリは Laravel アプリ本体です。起動手順はリポジトリルートの `README.md` を参照してください。

## 主要ルート
- `GET /login`
- `GET /dashboard/calendar`
- `GET /dashboard/customers`
- `GET /dashboard/settings`

## フロント
- ビルド：`npm run build`
- 開発：`npm run dev`
